# TRACEABILITY_POLICY

Canonical runtime rules live in `governance/workflow_protocol.md`.

IDs:
- `PR-*`, `SYS-*`, `SWR-*`, `SD-*`, `DD-*`
- `TC-U-*`, `TC-I-*`, `TC-S-*`, `TC-A-*`
- `DEF-*`, `CR-*`

Required chain:
- `PR -> SYS -> SWR -> SD|DD -> artifact -> TC -> DEF|none -> final status`

Matrix columns:
- product requirement ids
- system requirement ids
- software requirement ids
- design ids
- artifact refs
- verification cases
- acceptance cases
- defect refs
- final status
- evidence ref
